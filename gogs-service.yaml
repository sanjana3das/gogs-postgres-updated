apiVersion: v1
kind: Service
metadata:
  name: gogs
  namespace: gogs

spec:
  type: NodePort

  selector:
    app: gogs

  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30030

    - name: ssh
      port: 22
      targetPort: 22
      nodePort: 30022
